// Renders the Tracker section
export function renderTrackerSection(mainContent) {
  mainContent.innerHTML = `
    <section class="tracker-section">
      <h2>Tracker</h2>
      <p>This is the Tracker section. Add your tracking UI here.</p>
    </section>
  `;
}
